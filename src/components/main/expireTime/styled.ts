import styled from '@emotion/styled';

export const ExpireTimeContainer = styled.div`
  width: 58%;
  display: flex;
  column-gap: 0.4rem;
`;

export const ExpireTimeTextWrapper = styled.span`
  display: flex;
  align-items: center;
  justify-content: center;
  width: 56%;
  /* padding: 0.34rem;j */
  height: 3rem;
  column-gap: 0.4rem;
  background-color: #252728;
  border-radius: 0.8rem;
`;

export const ExpireTimeText = styled.span`
  font-size: 1.1rem;
  font-weight: 700;
`;
